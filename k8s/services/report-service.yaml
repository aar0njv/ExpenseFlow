apiVersion: v1
kind: Service
metadata:
  name: report-service
  namespace: expenseflow
  labels:
    app: report-service
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: report-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: report-service
  namespace: expenseflow
  labels:
    app: report-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: report-service
  template:
    metadata:
      labels:
        app: report-service
    spec:
      imagePullSecrets:
        - name: gitlab-registry-secret
      containers:
        - name: report-service
          image: registry.gitlab.com/aar0njv-projects/expenseflow/report-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: expenseflow-secret
                  key: REPORT_DATABASE_URL
            - name: ACCOUNT_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: expenseflow-config
                  key: REPORT_ACCOUNT_SERVICE_URL
            - name: TRANSACTION_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: expenseflow-config
                  key: REPORT_TRANSACTION_SERVICE_URL
